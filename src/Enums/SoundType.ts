// src/Enums/SoundType.ts

export enum SoundType {
    Engine = 'engine',
    Afterburner = 'afterburner',
    Wind = 'wind',
    Explosion = 'explosion',
    Speech = 'speech',
    Fire = 'fire',
    // Add other sound types as needed
}
